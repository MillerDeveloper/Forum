import { CheckMoodPipe } from './check-mood.pipe';

describe('CheckMoodPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckMoodPipe();
    expect(pipe).toBeTruthy();
  });
});
